
package teoria_programacion_secuencial;

public class Precedencia_Logicos_Conversion_Tipos {
    public static void main(String[] args) {
        //operadores logicos
        /*
        & y && - --> el ultimo es mas eficiente
        | y || --> el ultimo es mas eficiente
        ^ op1 ^ op2 Devuelve true si sólo uno de los operandos es true
        5.6 Operadores de bits
        Uso con las tarjetas.
        Los operadores a nivel de bits se caracterizan porque realizan operaciones sobre números enteros (o
        char) en su representación binaria, es decir, sobre cada dígito binario.
       
        5.7 Trabajo con cadenas
        String palabra= "blabla";
        String palabra2= "hola";
        String palabra3= palabra.concat(palabra2); //esto es lo mismo que +
        //System.out.println("total de la longitud de cadena: " + palabra.length()); //saber longitud
        //System.out.println(palabra3);
       
       
        5.8 Precedencia de operadores
        El orden de precedencia de los operadores determina la secuencia en que deben realizarse las
operaciones cuando en una expresión intervienen operadores de distinto tipo.
       
       
        6. Conversión de tipo.
        Las conversiones de tipo se realizan para hacer que el resultado de una expresión sea del tipo que
nosotros deseamos, en el ejemplo anterior para hacer que el resultado de la división sea de tipo real
y, por ende, tenga decimales.
       
        Repaso de cast
        int num1= 8;
        int num2= 16;
        //double d1=  num1 / num2; 0.0
        double d1= (double)num1 / num2;
        System.out.println(d1);
       
        6.1.2 - Conversión de números en Coma flotante (float, double) a enteros (int)
        double num=3.5;
x=Math.round(num); // x = 4
y=Math.ceil(num); // y = 4
z=Math.floor(num); // z = 3
       
        6.1.3 - Conversiones entre caracteres (char) y enteros (int)
            int num;
    char c;
    num = (int) 'A'; //num = 65
    c = (char) 65; // c = ‘A’
    c = (char) ((int) 'A' + 1); // c = 'B'
   
        System.out.println(c);
       
         */
    }
}
